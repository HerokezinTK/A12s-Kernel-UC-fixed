name: A127F-Makefile CI

on:
  push:
    branches: [ "A12s-Kernel-UC-fixed" ]
  pull_request:
    branches: [ "A12s-Kernel-UC-fixed" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
